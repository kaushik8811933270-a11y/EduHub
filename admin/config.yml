backend:
  name: git-gateway
  branch: main

media_folder: "images"

collections:
  - name: "notes"
    label: "Notes"
    folder: "notes"
    create: true
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Content", name: "body", widget: "markdown"}
